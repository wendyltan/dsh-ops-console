# dsh-ops-console

> DeepSeek Harness 的浏览器/手机运行帮助台：告诉你现在能不能用、发生了什么，以及下一步该做什么。

## 为什么需要它

当网页打不开、历史加载失败，或手机无法访问时，用户不应该先理解端口、日志或守护进程。dsh-ops 把这些检查整理成简单结论：

1. **现在怎么样**：是否可以继续使用。
2. **检查问题**：按遇到的现象给出影响和下一步。
3. **发生了什么**：用自然语言回顾最近的检查和访问变化。
4. **在另一台设备上打开**：准备或关闭手机、另一台电脑的访问地址。
5. **生成诊断报告**：导出一份默认脱敏、可发给技术支持的报告。

默认界面不会展示模块数、端口、进程号、原始日志、协议或其他实现细节。需要排障时再展开“技术详情”。

## 产品边界

| 项目 | 负责什么 |
| --- | --- |
| **dsh-ops-console** | 浏览器/手机状态说明、问题检查、事件时间线、脱敏诊断报告、可选远程访问设置 |
| **[dsh-desktop](https://github.com/wendyltan/dsh-desktop)** | macOS 本机打开 Harness、引擎更新和服务恢复 |

dsh-ops 可以独立安装和使用。它不安装、更新、重启或恢复 dsh 引擎，也不读取、控制或依赖 Guardian。即使没有桌面端，状态、诊断、报告和远程访问说明仍可工作。

## 使用

打开 dsh web，在 **设置 → 运行帮助** 中进入。

### 现在怎么样

打开就能看到一句结论，例如“现在可以正常使用”。正常时无需操作；有疑问时点 **检查问题**。

### 检查问题

选择最接近的现象：

- 网页打不开或一直转圈
- 历史记录加载失败
- 手机或另一台电脑打不开
- 使用时变慢或经常断开
- 其他问题

结果固定说明“结论、影响、下一步”。例如：本机可用但尚未设置其他设备访问，不会影响在这台 Mac 上使用。

### 发生了什么

这里记录用户可理解的检查和访问变化。点击 **生成报告** 可下载诊断 JSON；报告不包含密钥、令牌、提示词、会话正文、完整配置或远程地址。

### 在另一台设备上打开

如已安装并登录 Tailscale，可在这里准备访问地址。远程设备更改设置时需要运维令牌：

- 首次从本机页面打开时，令牌会自动准备。
- 仅将令牌粘贴到你信任的设备。
- 令牌只保存在本机文件（权限 0600）和该浏览器的本地存储中。

“允许哪些地址访问”属于高级选项。保存后需要由你在本机重新启动服务才会生效；dsh-ops 不会替你重启服务。

## 安装

前置条件：已安装 DeepSeek Harness（Node 18 或更高）。

    dsh plugin --profile web add dsh-ops-console

也可从 GitHub 或本地目录安装：

    dsh plugin --profile web add github:wendyltan/dsh-ops-console
    dsh plugin --profile web add /path/to/dsh-ops-console

安装后按你的现有方式重新打开 dsh web。

## 隐私与安全

- 所有写入操作要求同源请求和运维令牌。
- 余额查询只在已配置 DeepSeek API Key 时可用；密钥不会返回、写入日志或包含在报告中。
- 远程地址校验只接受 host 或 host:port，避免写入无效配置。
- 诊断报告只含状态结论、有限的事件时间线和版本信息。

## 开发与验证

    node scripts/verify.mjs

验证会检查语法、浏览器模块加载、HTTP 路由及临时 dsh web 烟雾启动。通过后可执行：

    node scripts/deploy.mjs

部署脚本只负责将经过验证的插件快照原子替换到本机部署目录；它不要求或调用 Guardian。正式服务何时重新打开由现有运行方式决定。

## 许可

[MIT](LICENSE)。
